using System.IO;
using UnityEngine;

public class FileDataHandler
{
/*    public FileDataHandler()
    {

    }

    public StoreIdsAvailable LoadAvailableData(string path) // Load available card id from json file
    {
        if (!File.Exists(path)) return null;

        string jsonString = File.ReadAllText(path);
        return JsonUtility.FromJson<StoreIdsAvailable>(jsonString);
    }

    public StoreIdsCurrent LoadCurrentData(string path) // Load current card id from json file
    {
        if (!File.Exists(path)) return null;

        string jsonString = File.ReadAllText(path);
        return JsonUtility.FromJson<StoreIdsCurrent>(jsonString);
    }*/
}
